# HANDOFF 2026-09-08 · 稀有发现修复 + 装备优化加固 + 一键导入仓库

> 本轮全部为本地未提交改动(等用户下令 commit)。上一轮遗留的税率报价修复(09-05,四处 ×1.05→÷0.95)仍未提交,见交接记忆。

## 修改一:稀有发现(includeRare)勾选失效五处修复(09-07)

用户反馈:制作木板不勾"稀有发现",稀有产物价格照算;勾/不勾收益完全一样。

根因:`CalculatorConfig.includeRare` 设计注释只写了"采集/炼金系",制造系从头就漏接。五个洞:

| #   | 位置                                 | 问题                                                                                                    | 修法                                                                                                                                                  |
| --- | ------------------------------------ | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | calculator/manufacture.ts            | ManufactureCalculator 不读 includeRare,精华(15%)+稀有掉落无条件算入产物                                 | 构造器读 `config.includeRare !== false`,productList 的 essenceDropTable/rareDropTable 两段用 if 包裹                                                  |
| 2   | apis/leaderboard/index.ts calcProfit | 炼金三件套(Transmute/Decompose/Coinify)创建时没传 includeRare                                           | 三件套+ManufactureCalculator 全部传入                                                                                                                 |
| 3   | 同文件 calcAllFlowProfit             | 函数不接收 includeRare(多步火车勾选无效)                                                                | 签名加参,两处 ManufactureCalculator 传入                                                                                                              |
| 4   | apis/favorite/index.ts               | 收藏表重建用收藏时快照,不跟当前勾选                                                                     | 重建前浅拷贝 config+subConfigs 覆盖当前值,不污染持久化数据                                                                                            |
| 5   | apis/manualchemy/index.ts            | 前端传了 includeTax/includeRare 但 API 无视;缓存无 key 勾了不重算;WorkflowCalculator 没传 sellTaxFactor | API 接收两参、缓存 key 化(game.ts get/setManualchemyCache 加 key 参数,参照 leaderboard 模式)、calcManualchemyProfit 加 sellTaxFactor/includeRare 参数 |

注意:enhancer/jungle/philosopher/enhancest/upgrade 页的 ManufactureCalculator **有意**不传 includeRare(默认算稀有),不是漏改。
dashboard/super-alchemy 的 cacheKey 原本就含 r 标志,无需改。

验证:eslint ✅ vue-tsc ✅ dev:3333 页面 200,浏览器勾选差异待用户最终确认。

## 修改二:装备优化页低强化推荐修复(09-08)

用户反馈:穿+12星空镇流器推荐+7同款;副手暴影之囊推荐无等级;副手类普遍如此。

根因(不在推荐算法,在数据源头):**MK助手(2.5.1 起就有)的 COMBAT_LOCS 把 off_hand/head/body/hands/feet/legs/back/neck/ring/charm/pouch 全部当战斗位排除**——但生活装备恰恰穿在这些通用槽上。玩家身上穿戴(含强化等级)从不进导出数据 → 利润网一键导入只能从背包(inventoryMap)自动挑货当现装 → 现装等级全是低的/默认的 → +7、"白板"都成了"提升"。用户感知的"MK助手导出数据不匹配"即此。

配套修复:

- apis/upgrade/index.ts:同款低强化不再参评(`sameHrid && level <= currentLevel` 跳过;原只跳 ===),现装等级数据缺失时不会再推荐同款低级
- pages/upgrade/index.vue:建议装备 evalLevel=0 时显示"白板"tag(原先什么都不显示,像数据缺失);词条已加 zh-tw/en

## 修改三:一键导入顺带收仓库(09-08)

pages/dashboard/components/ActionConfig.vue processImportData:检测到 `data.warehouseMap`(MK助手 3.1+ 导出)时,清洗(hrid 在游戏数据中且数量>0)后写入 warehouse 页的 `warehouse-map`/`warehouse-imported-at` memory。**效果:一键导入一次,玩家配置+仓库数据全部进站,仓库页不用再手动粘贴**。

## 配套插件:利润网助手-3.1.1(桌面\脚本\利润网\,只备文件未发布)

- COMBAT_LOCS 缩减为 main_hand/two_hand/trinket/inventory,身上全部穿戴(含强化等级)完整导出——修改二/三生效的前提
- node --check 语法 ✅;发布走用户手动 GreasyFork(红线:不上线不推送)

## 用户侧动作

1. 游戏内装 3.1.1 → 刷新 → 利润网"一键导入"重新导入一次(装备等级/仓库全对)
2. 装备优化页复查:现装应显示真实强化等级,不再推荐同款低级

## 改动文件清单(git)

manufacture.ts / favorite/index.ts / leaderboard/index.ts / manualchemy/index.ts / upgrade/index.ts / game.ts / ActionConfig.vue / upgrade/index.vue / en.ts / zh-tw.ts(+自动生成 components.d.ts)
另有三个**不属于本轮**的既有零星改动随仓库挂着:deploy.yml、components.d.ts、public/CNAME
