# 全市场赚钱路线：验收记录

日期：2026-09-22
工作区基线：`81a715c` 之后的未提交实现。

## 扫描范围

- 单步/制作、制作炼金、多步炼金、直接强化、制作强化、继承强化。
- 默认即时价格、计税、含稀有发现、只显示正利润且报价可执行的路线。
- 低级火车仅在用户提供完整材料 HRID 集合后排除；只排除全部步骤均命中的多步纯材料制作链。

## 自动化结果

| 检查                           | 结果                                   |
| ------------------------------ | -------------------------------------- |
| `vitest run tests/opportunity` | 通过（4 files, 10 tests）              |
| `vitest run`                   | 通过（10 files, 31 tests）             |
| `vue-tsc`                      | 通过                                   |
| ESLint（本次改动文件）         | 通过                                   |
| `vite build --mode public`     | 通过；按用户授权，机会页已进入公开产物 |
| `vite build --mode private`    | 通过                                   |

## A01～A21 状态

- A02–A05、A08–A10：以固定小型计划测试税费、硬币免税、整链时薪、内部转移、缺价状态及炼金起点的全链时薪选择。
- A17：覆盖空规则、纯材料链排除和混合链保留；页面显示命中数量及最多五个样例。
- A20：覆盖工作流外部边界，确认中间物料不会重复买卖／计税。
- A19：generation 取消和炼金节点上限已有实现，但尚无独立异步夹具。
- A01、A06–A07、A10–A16、A18、A21 的完整固定夹具、性能和浏览器交互：未验证。

## 已知限制

扫描会串行绑定不可变的游戏数据／行情副本，并把行情内容指纹、人物状态指纹、买卖场景、税费、稀有开关、路线范围及节点上限写入快照 ID 和导出文件。强化等级价格也读取该副本。

多步炼金起点已按扣除买入后的整链时薪选动作，但子分支仍沿用已有 `smart` 决策，尚未实现文档要求的有界多候选穷举与全链时薪支配剪枝。因此结果不得称为“声明范围内全局最优”；这是当前未完成验收的阻塞项。
