# 全市场赚钱路线：审查记录

日期：2026-09-22

## 本次修复

- [授权变更] 用户明确要求公开部署；机会页已移至 `public.ts`，公开构建包含该功能。私有路由虚拟模块替换仍保护其余私有路由。
- [P1] 空路线选择：空数组现在表示不扫描，不再悄悄恢复为全量扫描。
- [P1] 路线去重：机会榜 ID 现在包含强化等级、保护、逃逸、催化剂与工作流步骤，避免不同策略相互覆盖。
- [P1] 报价状态：待补价路线保留状态，榜单默认只显示 `priced`，可取消该过滤查看待补价候选。
- [P1] 交互：加入取消、快照标识、路线族原始候选计数、纯数据 JSON/CSV 导出、多步炼金链详情，以及显式材料 HRID 的纯低级火车排除和样例预览。
- [P0] 快照串用：机会扫描串行绑定不可变游戏／行情副本；强化等级价格路径也改为读取该副本，快照 ID 包含行情内容和人物状态指纹。
- [P0] 工作流重复计价：适配器改用 `WorkflowCalculator` 已抵消的外部收支边界，子步骤仅展示，避免把中间品按市场价格再次买卖和计税。
- [P1] 炼金明细：将根采购、步骤成本、最终出售及环截断出售归入统一计划；子节点缺失报价向上标记为待补价。

## 已验证

- `vue-tsc` 通过。
- `vitest run` 通过（10 files、31 tests；机会模块单独运行 4 files、10 tests）。
- public/private 两种 Vite 构建通过；公开产物已检索到“全市场机会”页面。

## 尚未验证或未完成

- 多步炼金起点已按扣除买入后的整链时薪选动作，但子分支仍沿用既有 `smart` 决策，尚未实现文档建议的有界穷举与全链时薪支配剪枝；不得宣称全局最优。
- 取消在路线族边界响应，不能中断单个同步旧计算器的内部循环。
- 低级火车规则已实现为明确 HRID 集合的纯材料链排除和样例预览；档位截止点尚未实现，不能擅自设定阈值。
- A01～A21 尚未全部建立独立的固定夹具自动测试；浏览器手工验收未经授权，未执行。
